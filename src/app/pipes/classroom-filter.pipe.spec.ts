import { ClassroomFilterPipe } from './classroom-filter.pipe';

describe('ClassroomFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassroomFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
